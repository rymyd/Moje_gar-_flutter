workflows:
  android-only:
    name: Build Android APK only
    environment:
      flutter: stable
      xcode: none          # vypne iOS část
    scripts:
      - name: Build release APK
        script: |
          flutter clean
          flutter pub get
          flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
